# Campus2Air — Local Development Environment Variables
# Copy this file to .env and fill in your values.
# .env is loaded automatically when running python app.py locally.

# ── Flask ────────────────────────────────────────────────────────────────────
FLASK_SECRET_KEY=change-me-to-a-random-secret

# ── Database ─────────────────────────────────────────────────────────────────
# Leave DB_ENGINE as "sqlite" for local dev — no extra setup needed.
DB_ENGINE=sqlite
# DATABASE_PATH=carpool.db   # default; change if you want a different location
# AUTO_INIT_DB=1             # optional; enable request-time schema bootstrap on non-local deployments

# ── MySQL (production / optional for local) ──────────────────────────────────
# Only needed if DB_ENGINE=mysql
# MYSQL_HOST=your-mysql-host
# MYSQL_USER=your-mysql-user
# MYSQL_PASSWORD=your-mysql-password
# MYSQL_DATABASE=carpool
# MYSQL_PORT=3306
# MYSQL_SSL=false

# ── Resend email notifications (optional — omit to disable email alerts) ──────
# Without these, email notifications are silently skipped and the app works normally.
# Sign up free at https://resend.com — 3,000 emails/month, no credit card needed.
# For testing, RESEND_FROM can stay as onboarding@resend.dev (Resend's shared domain).
# For production, verify your own domain and update RESEND_FROM.
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# RESEND_FROM=Campus2Air <onboarding@resend.dev>

# ── Local dev notes ──────────────────────────────────────────────────────────
# 1. Run: pip install -r requirements.txt
# 2. Run: python app.py   →  http://localhost:8000
# 3. For Google Sign-In locally, add "localhost" to Firebase authorized domains:
#    Firebase Console → Authentication → Settings → Authorized domains → Add domain
